node_modules
.pnpm-store
.turbo
coverage
dist
build
*.tsbuildinfo
.DS_Store
*.log
.env
.env.*
!.env.example

# SQLite is a rebuildable delivery artifact. Player JSON API resources are the
# canonical data and are intentionally kept under version control.
data/sqlite/*.sqlite
data/sqlite/*.sqlite-*
